/**
 * Infitie loops
 */

// infinite while-loop
while (true) {
    // statement(s)
    // check some external condition like end of file, network, user input, etc.
    if ( /* external-condition-met */ ) {
        break;
    }
}

// infinite for-loop
for (;;) {
    // statement(s)
    // check some external condition like end of file, network, user input, etc.
    if ( /* external-condition-met */ ) {
        break;
    }
}
